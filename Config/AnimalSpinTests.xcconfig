// AnimalSpinTests.xcconfig — settings for the unit-test bundle, in every configuration.

PRODUCT_BUNDLE_IDENTIFIER = com.circuitstitch.toys.animals.tests

// Xcode synthesizes the test bundle's Info.plist; there is no file to check in.
GENERATE_INFOPLIST_FILE = YES
